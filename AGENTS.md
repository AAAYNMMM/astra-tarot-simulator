# 星纱塔罗项目 AI 开发指引

本文件是所有后续 AI、Codex 或自动化开发代理进入仓库后必须首先读取的工作入口。

## 1. 必读顺序

开始任何开发任务前，按以下顺序读取：

1. `README.md`
2. `docs/DECISIONS.md`
3. `docs/ENGINE_ARCHITECTURE.md`
4. `docs/CARD_DATA_STANDARD.md`
5. `docs/ROADMAP.md`
6. `docs/PROGRESS.md`
7. 与当前任务直接相关的代码、数据和测试

不得仅依据用户当前一句话直接修改项目。先确认锁定决策、当前阶段、已完成任务和下一任务。

## 2. 用户命令约定

### “开始任务”

含义：

1. 读取上述全部文档。
2. 从 `docs/PROGRESS.md` 中寻找状态为 `NEXT` 的任务。
3. 对照 `docs/ROADMAP.md` 核实任务依赖和验收标准。
4. 若依赖满足，直接开始该任务。
5. 完成代码、数据、测试和必要文档更新。
6. 更新 `docs/PROGRESS.md`，记录完成情况、验证结果和新的 `NEXT` 任务。

### “继续任务”

含义：

1. 读取上述全部文档。
2. 优先恢复 `docs/PROGRESS.md` 中状态为 `IN_PROGRESS` 或 `BLOCKED` 的任务。
3. 检查仓库实际状态，不能只相信旧进度文字。
4. 从最后一个未完成验收项继续。
5. 完成后更新进度和验证记录。

若没有 `IN_PROGRESS` 任务，则把 `NEXT` 任务视为继续目标。

## 3. 已锁定的产品边界

以下决策除非用户明确修改，否则不得改变：

- 运行时不得调用 AI 大模型或外部生成式 AI 服务。
- 用户只能选择固定预设问题，不提供自由文本问题解析。
- 预设问题应覆盖大多数常见占卜场景。
- 四种牌阵保持不变：心语单张、时间之流、五牌十字、凯尔特十字。
- 不得新增、删除、重命名或改变这四种牌阵的牌位结构。
- 抽牌必须真实随机，解读不得根据问题偷偷改牌或重抽。
- 解牌由可检查、可复现、可测试的算法规则完成。
- 单牌资料质量、多牌综合能力和预设问题贴合度的目标评分均为 9.0/10 以上。
- “准确”指牌义质量、问题贴合、一致性、证据链和条件表达，不宣称科学验证的超自然预测准确率。

完整决策见 `docs/DECISIONS.md`。

## 4. 工作原则

- 一次只推进一个明确任务 ID，避免同时改动多个阶段。
- 先建立数据契约和测试，再批量填充 78 张牌资料。
- 不允许用大段不可计算文本替代结构化语义字段。
- 重要结论必须能够追溯到问题配置、牌位、牌面语义和关系规则。
- 不得使用“正位多所以好、逆位多所以坏”一类粗糙总分算法。
- 不得把全部大阿卡纳作为第五元素参与花色优势统计。
- 规则冲突必须被识别、分层或转化为条件结论，不能随意忽略。
- 任何批量数据变更都必须经过数据契约测试、完整性测试和抽样人工审查。
- 保持完全离线运行，不引入不必要的大型依赖。

## 5. 任务完成定义

任务只有同时满足以下条件才可标记为 `DONE`：

1. 任务范围内的实现或文档已经提交。
2. 对应验收标准全部满足。
3. 自动测试通过，或明确记录无法运行的原因。
4. 人工抽样检查完成，若任务要求人工审查。
5. 没有破坏锁定决策。
6. `docs/PROGRESS.md` 已更新。
7. 新增设计决策已写入 `docs/DECISIONS.md`，而不是只留在聊天记录里。

## 6. 进度文档维护规则

每次任务结束必须在 `docs/PROGRESS.md` 更新：

- 当前阶段
- 当前任务 ID 与状态
- 本次完成内容
- 修改或新增文件
- 测试命令与结果
- 未解决问题
- 下一任务 ID
- 更新时间

状态只允许使用：

- `BACKLOG`
- `NEXT`
- `IN_PROGRESS`
- `BLOCKED`
- `DONE`
- `DEFERRED`

同一时间原则上只能存在一个 `IN_PROGRESS` 和一个 `NEXT` 任务。

## 7. 交付约束

- 讨论阶段不要擅自生成 Codex 提示词或命令。
- 修改代码时只交付本次修改或新增的文件，不额外制作完整项目压缩包。
- 不为了“显得高级”引入 Neo4j、Drools、多智能体或云服务。项目当前规模优先使用原生 JavaScript、Python 标准库、JSON 和现有测试体系。
- 如果代码实际状态与文档冲突，以代码和测试为事实来源，同时修正文档。
